{
  "routes": [
    { "handle": "filesystem" },

    {
      "src": "^/([^/.]+)/([^/.]+)$",
      "dest": "/imovel.html?corretor=$1&imovel=$2"
    },

    {
      "src": "^/([^/.]+)$",
      "dest": "/painel.html?corretor=$1"
    }
  ]
}
